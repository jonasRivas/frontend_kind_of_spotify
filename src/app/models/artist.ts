export class Artist {
    constructor(
        // public _id: string, //en este caso de omite para hacerlo sencillo
        public name: string,
        public definition: string,
        public image: string
    ) { }
}